# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このディレクトリの役割

ETロボコン（チーム名: **SOROT☆SCSKQ**）の **モデル審査提出物** とその検討資料の管理ディレクトリ。

- **参加クラス**: **アプライドクラス**（HackSPi = SPIKE + Raspberry Pi / RasPike-ART構成）
- **モデル審査対象課題**: 規約から判断すること。もし、選択性であれば、メンバーに確認を取ること。
- **目標**: CS大会出場・完走・高スコア・モデル審査高評価

## ディレクトリ構造

```
/
├── CLAUDE.md               # このファイル
├── GEMINI.md               # CLAUDE.md参照と書かれているだけ
├── Reference/              # 公式PDFを格納（変更しない）
├── 要求モデル/                # アプライド審査の「要求モデル」(A3横1枚)
├── システム分析モデル/          # 次フェーズ（未着手）
├── 設計モデル/                # 次フェーズ（未着手）
└── 制御モデル/                # 次フェーズ（未着手）
```

## 提出物の全体像（アプライドクラス CS大会）

A3横 6枚。それぞれが独立ファイル/フォルダに対応。

| モデル | フォルダ | 主な図 |
|---|---|---|
| アブストラクトページ | （未作成） | 自然言語+俯瞰図 |
| **要求モデル** | `要求モデル/` | UC図 / SysML要求図 / アクティビティ図 |
| システム分析モデル | `システム分析モデル/` | コンポーネント図 / BDD / IBD / シーケンス図 |
| 設計モデル | `設計モデル/` | クラス図 / **ステートマシン図(必須)** |
| 制御モデル | `制御モデル/` | アクティビティ図 / ブロック線図 |

## 重要な前提（規約を読まなくても知っておくこと）

### 採点・走行
- Referenceにある競技規約から考えること

### 攻略対象
- Referenceにある競技規約から考えること
- 攻略対象はアプライドクラスとなる。それ以外のクラスは不要。

### 重大事項
- 規約を読み込み、高得点に繋がるポイント、低得点に繋がる注意ポイントはmdにまとめること。
- 品質は **複数側面** から検討必須（信頼性・効率性・保守性・移植性など）
- 全モデル間の **段階的・双方向トレーサビリティ** が審査基準
- モデルとソフトウェア実装は **機能・構造・振舞いで関連** していること（規約 §3.2）

## 命名規則（要求モデル内で確立済）

| 種別 | プレフィックス | 例 |
|---|---|---|
| 要求 | `REQ-` | `REQ-401` (機能要求 4xx / 性能要求 5xx) |
| ユースケース | `UC-` | `UC-201` |
| 品質要求 | `QA-<カテゴリ>-` | `QA-PERF-01`, `QA-REL-02` |
| 制約 | `C-<区分>-` | `C-101` (規約1xx / 物理2xx / 開発3xx) |

ID重複・採番衝突に注意。新規追加は `要求モデル/00_要求モデル構成.md` の一覧表に必ず追記する。

## ノート作成ルール

### フロントマター（全 .md ファイル必須）

```yaml
---
title: <ファイル名と同じ>
tags:
  - etrobocon
  - 要求モデル        # or 該当モデル名
  - アプライドクラス
  - <その他トピック固有タグ>
---
```

### UML/SysML図の作成方針（重要）
- **UML/SysML の図そのものはここでは描画しない。**
- 各図に対応する `.md` ファイル（`01_SysML要求図.md` / `02_UMLユースケース図.md` 等）は **「描くべき要素・関係性の構成仕様」** を表形式で記述する
- 図を更新する場合は `.md` の構成仕様を先に更新 → 人間が図を再描画 という流れ

### クロスリファレンス
- 同ボルト内: ウィキリンク `[[ファイル名]]`（拡張子不要）
- リポジトリ全体での横断参照は `[[02_idea/...]]` のように相対パスを含める

### Mermaid
- ワークフロー図は Mermaid で記載可。詳細は書きすぎず概要のみ

## 関連ドキュメント

プロジェクト内共通資産 (来年度への引き継ぎ用資料):
- `/appendix/`: 来年度以降も対話型AIアシスタント（エージェント）と連携して高品質なモデリングを行うための、モデル別に分割された指示ガイドライン群（UMLやFMEA、要求モデルの指示テンプレートが格納されています）。

外部リソース:
- 走行体ソフトウェアプラットフォーム: https://github.com/ETrobocon/RasPike-ART
- NotebookLM: `仕様整理_2026` / `モデル採点基準` ノート（PDF→要求項目抽出に活用）

## チーム運営の前提

- **役割**: シミュレーター要員 / 戦略・モデル作成担当 / 難所フォロー
- **ツール**: Teams（連絡）、M365 Loop（意思決定ログ）、Google Drive（資料）、NotebookLM（仕様確認・採点）、GitHub（WBS・ソース）
- **詰まりルール**: 2時間解決しなければ Teams `#general` に報告
- **意思決定**: 方針・仮説・確認指標の3点セットを記録（[[04_knowledge/etrobocon-team-guideline]]）

## 次のステップ（要求モデル完了後）

優先順位は未確定。チームと相談すべき。

1. **SysML、UMLをメンバーが描画**（[[01_SysML要求図]] / [[02_UMLユースケース図]] などの構成仕様を入力に）
2. **アブストラクトページ** — 4モデルの俯瞰 + トレーサビリティ概観（1枚目）
3. **システム分析モデル** — 走行体+無線通信デバイスの BDD/IBD、I/F定義（連携明示が審査の生命線）
4. **設計モデル** — 重要1サブシステム選定、クラス図、ステートマシン図（必須）
5. **制御モデル** — ETラリー要素技術（画像処理・AES復号・経路計画）の検討
   - 信頼性根拠の FMEA は `要求モデル/04_FMEA.md` に既にあるので、制御モデルでは対策設計に集中

## 自分（Claude）への注意

- **UML・分析設計のモデリング基本方針・ガイドラインの遵守** → 要求モデル、ユースケース図、シーケンス図、FMEA等の作成・修正・レビューを行う際は、必ず `/Appendix/` ディレクトリに格納されている、対象モデルの基本方針やガイドライン（例：`00_基本方針.md` や `02_UMLユースケース図指示ガイド.md` など）のルールを読み込み、それらに厳密に準拠すること。
- **PDF直読みは失敗する** → 必ず上記 `pdftotext` フローを通す
- **PlantUML / `.puml` ファイルは作らない** → UML/SysML図はメンバーが描く。`.md` には「構成仕様（要素・関係の一覧表）」を書く
- **モデル審査対象** → 規約から判断すること。もし、選択性であれば、メンバーに確認を取ること。
- **走行体+無線通信デバイスの2システム連携** をモデル全段階で意識（要求段階から見えるように）
- ID一覧の一次管理元:
  - REQ-XXX → `要求モデル/01_目標.md`（目標・ステークホルダー要求）
  - UC-XXX → `要求モデル/02_UMLユースケース図.md`
  - QA-XXX → `要求モデル/06_品質特性.md`
  - C-XXX → `要求モデル/08_制約.md`
  - FM-XXX → `要求モデル/04_FMEA.md`
- 規約PDF章節を参照するときは出典（ファイル名と§章節番号）を併記する
- FMEA は信頼性 QA-REL-* の達成根拠として位置づける（制御モデルにも引き継ぐ）
- **2025 CS審査総評** からの学び（`/tmp/etrc_pdf_text/ETRC2025_*.txt`）:
  - 用語定義はアドバンスト CS 0/6 のみ取り組み → 差別化要因
  - 可読性で 6/6 中 5チーム減点 → 印刷時の文字サイズ最重要
  - 上位3チームでも「サブシステム設計・アーキテクチャ」「制御戦略・要素技術」「概要・全体トレサビ」に課題
  - アブストラクトページは「本文のモデル」「特徴・結果・効果が一読でわかる」が高評価

### 要求モデル
#### ユースケース図
- **UC粒度ポリシー**: Cockburn の Sea level（user goal）に揃える。Fish level（「ログインする」「読み取る」等の手段）は **UC図に書かず** ユースケース記述の基本フローに含める。ETロボコン2025 CSワークショップ P60 の基本UC図に倣う
